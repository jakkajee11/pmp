/**
 * Settings Feature - Public Exports
 *
 * Constitution: Implements US12 (Localization) - User preferences and language settings
 */

// Types
export * from './types';

// Components
export { LanguageSelector, LanguageSelectorFull } from './components/language-selector';

// Hooks
export { useSettings } from './hooks/use-settings';
